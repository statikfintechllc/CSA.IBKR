# SFTi.IOS/trades — Trade Execution

Order placement, modification, cancellation, and position management via
the IBKR CP Gateway REST API.  Order history is persisted client-side in
the Vault.

See `trades.js` for the full API.
