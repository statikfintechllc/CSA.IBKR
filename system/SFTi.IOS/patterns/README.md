# SFTi.IOS/patterns — Technical Pattern Recognition

Client-side detection of classic chart patterns on OHLCV data:
head-and-shoulders, double top/bottom, cup-and-handle, ascending/
descending channels, and triangle formations.

All pattern detection is local — no server calls required.

See `patterns.js` for the full API.
